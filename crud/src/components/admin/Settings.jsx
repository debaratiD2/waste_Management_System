import React from 'react';

const Settings = () => {
  return (
    <div>
      <h4>System Settings</h4>
      <p>Settings for managing collection routes, frequencies, etc...</p>
    </div>
  );
};

export default Settings;
